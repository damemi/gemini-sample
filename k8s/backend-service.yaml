apiVersion: v1
kind: Service
metadata:
  name: gemini-sample-backend
  labels:
    app: gemini-sample-backend
spec:
  type: ClusterIP
  selector:
    app: gemini-sample-backend
  ports:
    - name: http
      port: 9090
      targetPort: 9090
