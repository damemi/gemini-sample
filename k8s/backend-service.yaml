apiVersion: v1
kind: Service
metadata:
  name: gemini-sample-backend
  labels:
    app: gemini-sample-backend
spec:
  type: ClusterIP
  selector:
    app: gemini-sample-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
